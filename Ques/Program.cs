public class Program
{
    public static void Main(string[] args)
    {
        // IList<Person> p=new List<Person>();
        // p.Add(new Person{Name="Avishek",Addres="Darjeeling",Age=10});
        // p.Add(new Person{Name="Ayush",Addres="Darjeeling",Age=27});
        // p.Add(new Person{Name="Adi",Addres="Darjeeling",Age=30});
        
        // PersonImplementation a=new PersonImplementation();
        // System.Console.WriteLine(a.GetName(p));
        // System.Console.WriteLine(a.Average(p));
        // System.Console.WriteLine(a.Max(p));

        // Source addition=new Source();
        // System.Console.WriteLine(addition.Add(1,2,3));
        // System.Console.WriteLine(addition.Add(2d,3,4));
    }
}